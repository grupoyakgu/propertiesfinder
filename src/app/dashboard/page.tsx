import { redirect } from "next/navigation";

export default function DashboardPage() {
  return (
    <div style={{ padding: "2rem" }}>
      <h1>Dashboard</h1>
      <p>Dashboard page is being updated for Supabase migration.</p>
      <p>See SUPABASE_MIGRATION.md for migration progress.</p>
    </div>
  );
}
